{
  "name": "todo-api",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/app.js",
    "dev": "nodemon -L ./src/app.js",
    "migrate:generate": "npx sequelize-cli migration:generate --name",
    "seed:generate": "npx sequelize-cli seed:generate --name",
    "migrate": "npx sequelize-cli db:migrate",
    "migrate:rollback": "npx sequelize-cli db:migrate:undo --name",
    "seed": "npx sequelize-cli db:seed --seed",
    "seed:rollback": "npx sequelize-cli db:seed:undo --seed"
  },
  "author": "trung.buitat",
  "license": "ISC",
  "dependencies": {
    "auto-bind": "^4.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "express-validator": "^6.14.0",
    "http-status": "^1.5.0",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.7.3",
    "sequelize": "^6.16.1"
  },
  "devDependencies": {
    "eslint": "^8.9.0",
    "lint-staged": "^12.3.4",
    "nodemon": "^2.0.15",
    "sequelize-cli": "^6.4.1"
  },
  "lint-staged": {
    "*.js": [
      "eslint"
    ]
  }
}
